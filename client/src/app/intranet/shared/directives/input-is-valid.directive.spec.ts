import { InputIsValidDirective } from './input-is-valid.directive';

describe('InputIsValidDirective', () => {
  it('should create an instance', () => {
    const directive = new InputIsValidDirective();
    expect(directive).toBeTruthy();
  });
});
